[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "heuristicos-pkg"
version = "0.1.0"
description = "Algoritmos heurísticos: TSP y Knapsack"
authors = [
    {name = "Tu Nombre"}
]
dependencies = []

[project.optional-dependencies]
test = ["pytest"]
